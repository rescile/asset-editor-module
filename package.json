{
  "name": "asset-editor-module",
  "private": true,
  "version": "0.0.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "package": "npm run build && zip -r asset-editor.zip app module.toml"
  },
  "devDependencies": {
    "vite": "^8.0.3"
  },
  "dependencies": {
    "papaparse": "^5.5.3"
  }
}
